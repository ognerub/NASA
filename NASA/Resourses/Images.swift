import UIKit

extension UIImage {
    static let backward = UIImage(named: "backward") ?? UIImage(systemName: "arrow.left")
    static let noImage = UIImage(named: "noImage") ?? UIImage(systemName: "nosign")
}
